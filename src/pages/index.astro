---
// This page exists only to redirect users to their preferred language.
// Astro's i18n routing determines the best locale and redirects automatically.
// No content is needed here. It will never be rendered.
return Astro.redirect(`/${Astro.currentLocale}/`);
---